import React, { PropTypes } from 'react';
import './style.less';

export default class AdminIndex extends React.PureComponent {
  static propTypes = {
  }

  render() {
    return (
      <div>AdminIndex</div>
    );
  }
}
