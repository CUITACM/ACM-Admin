import React, { PropTypes } from 'react';
import './style.less';

export default class AdminSpider extends React.PureComponent {
  static propTypes = {
  }

  render() {
    return (
      <div>AdminSpider</div>
    );
  }
}
